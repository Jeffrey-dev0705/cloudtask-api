# Terraform

Placeholder for cloud IaC. No modules are checked in yet.

Likely targets when added:

- AWS: VPC, EKS, RDS PostgreSQL, ElastiCache Redis, ECR
- Azure: VNet, AKS, Azure Database for PostgreSQL, Azure Cache for Redis, ACR

Use remote state and the cloud secret manager for credentials.